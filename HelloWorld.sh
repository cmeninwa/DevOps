#!/bin/bash

echo "Hello World"

echo " Chike's babe "

echo -n "How"                               ''' The -n param means that this should be printed in a new line and by default it will print " How are you" cos the echo line below does not specify new line '''
echo "are you"

''' echo in bash is print in other languages '''


## Commands that should be ran directly on the cli can now be embedded into scripts such as those with the .sh extensions so that when
## when run from the command line, they output all of the expected output of the commands at once...